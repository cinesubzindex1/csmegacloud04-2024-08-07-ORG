<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= file.name %></title>
    <link rel="stylesheet" href="/css/dl.css">
    <meta http-equiv="refresh" content="100; URL=https://outrightsham.com/qj3u0dhi2?key=aa36ab12708b3d7b7b9ef30a1334084d" />
</head>
<body>
    <div class="container">
        <div id="box">
            <div class="download-section">

                <h4>ඩිරෙක්ට් ඩවුන්ලෝඩ් කිරීමට ඉහත Direct Download හෝ Google Server එක ක්ලික් කර ලබා ගන්න.</h4>

                <p class="file-info">File Name: <span><%= file.name %></span></p>
                <p class="file-info">File Size: <span><%= file.size %></span></p> 
                <div id="dl-links"></div>
            </div>

            <div class="additional-info">
                <h2>🔰 Help 🔰</h2>
                <p><%= info.main[0] %></p>
                <p><%= info.main[1] %></p>
                <p><%= info.main[2] %></p>
                <p>If Any Issue Please Contact <a href="<%= info.help %>">Admin</a>
            </div>
        </div>
        <div id="count">
            <h4>Cinesubz Download Page</h4><h1 id="second"></h1>
        </div>
    </div>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            let countdown = Number('<%= timer.time %>');
            let active = '<%= timer.active %>'

            const timerElement = document.querySelector("#second");
            timerElement.textContent = `Please wait ${countdown} seconds...`;

            const downloadSection = document.querySelector(".download-section");

            function applyUrl() {
                document.querySelector("#box").style.display = "block";
                document.querySelector("#count").style.display = "none";
            const dlBtn = document.querySelector("#dl-links");

                dlBtn.innerHTML += '<a href="<%= sUrl %>" class="download-btn" id="link1">Direct Download</a>';
                dlBtn.innerHTML += '<a href="<%= gUrl %>" class="download-btn" id="link2" target="_blank">Google Download</a>';
            }

            if(active == 'true') {
            const intervalId = setInterval(function() {
                countdown--;
                timerElement.textContent = `Please wait ${countdown} seconds...`;
                if (countdown <= 0) {
                    clearInterval(intervalId);
                    applyUrl();
                }
            }, 1000);
        } else {
            applyUrl();
        }
        });
    </script>
</body>
</html>
